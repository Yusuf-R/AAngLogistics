import { Text } from 'react-native';
import { useProtectedScreen } from '../../../hooks/useProtectedScreen';

export default function ClientDashboard() {

    return <Text className="text-center mt-10">Client Dashboard</Text>;
}
